module MakeCode (codes, codeTable, cds, module Frequency) where 
import Types
import Frequency 
import MakeTree
import CodeTable 

-- they are similar but difference made by code written

codes :: [Char] -> Tree
codes = makeTree.frequency


cds = mTree.frequency

